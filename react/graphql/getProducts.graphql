query product($slug: String) {
    product(slug: $slug) {
        recommendations {
            similars {
                linkText
                productName
                items {
                    images {
                        imageLabel
                        imageText
                        imageUrl
                    }
                }
            }
        }
    }
}
